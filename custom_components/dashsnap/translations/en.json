{
  "config": {
    "step": {
      "user": {
        "title": "DashSnap",
        "description": "DashSnap base URL is auto-detected when possible and pre-filled below — confirm or edit it. Examples.",
        "data": {
          "base_url": "DashSnap base URL"
        }
      }
    },
    "error": {
      "invalid_url": "Enter a full URL with a valid scheme (http or https), host, and port.",
      "cannot_connect": "Could not reach DashSnap at that URL. Check the host/port and that DashSnap is running.",
      "app_unhealthy": "Reached DashSnap, but /health failed — verify DashSnap's targets configuration."
    },
    "abort": {
      "already_configured": "DashSnap is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "DashSnap",
        "description": "Change the DashSnap base URL if autodetection picked the wrong one. It is validated and health-checked before saving. Examples.",
        "data": {
          "base_url": "DashSnap base URL"
        }
      }
    },
    "error": {
      "invalid_url": "Enter a full URL with a valid scheme (http or https), host, and port.",
      "cannot_connect": "Could not reach DashSnap at that URL. Check the host/port and that DashSnap is running.",
      "app_unhealthy": "Reached DashSnap, but /health failed — verify DashSnap's targets configuration."
    }
  },
  "services": {
    "record": {
      "name": "Record URL",
      "description": "Record any web page to video or screenshot via DashSnap. Provide a full URL.",
      "fields": {
        "url": {
          "name": "URL",
          "description": "Full URL to capture."
        },
        "target": {
          "name": "Target",
          "description": "Named target from DashSnap config (e.g. \"infoshare\"). Defaults to first configured target."
        },
        "seconds": {
          "name": "Duration (seconds)",
          "description": "Video length. Ignored when format is png."
        },
        "format": {
          "name": "Format",
          "description": "webm video or a single png screenshot."
        },
        "viewport_width": {
          "name": "Viewport width",
          "description": "Render width in pixels."
        },
        "viewport_height": {
          "name": "Viewport height",
          "description": "Render height in pixels."
        }
      }
    },
    "record_ha": {
      "name": "Record HA dashboard",
      "description": "Record a Home Assistant page. Provide a path — the HA base URL is applied automatically from the target config.",
      "fields": {
        "path": {
          "name": "Page path",
          "description": "HA route to capture, e.g. /lovelace/0 or /entity-availability-test/entity-availability-test."
        },
        "target": {
          "name": "Target",
          "description": "Named HA target from DashSnap config (e.g. \"ha\"). Defaults to first configured target."
        },
        "seconds": {
          "name": "Duration (seconds)",
          "description": "Video length. Ignored when format is png."
        },
        "format": {
          "name": "Format",
          "description": "webm video or a single png screenshot."
        },
        "viewport_width": {
          "name": "Viewport width",
          "description": "Render width in pixels."
        },
        "viewport_height": {
          "name": "Viewport height",
          "description": "Render height in pixels."
        }
      }
    }
  }
}